name: Mobile CI

on:
  push:
    branches: [master]
    paths: ['mobile/**']
  pull_request:
    branches: [master]
    paths: ['mobile/**']

jobs:
  validate:
    name: TypeScript & Build Check
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: mobile/package-lock.json

      - name: Install dependencies
        working-directory: mobile
        run: npm ci

      - name: TypeScript type check
        working-directory: mobile
        run: npx tsc --noEmit

      - name: Wiring audit (nav targets ↔ screens, api.ts ↔ backend routes)
        run: |
          node backend/scripts/wiring_audit.js | tee /tmp/wiring.txt
          if grep -q "❌" /tmp/wiring.txt; then
            echo "::error::Wiring audit found orphaned navigation targets or API calls"
            exit 1
          fi

      - name: Expo export check (web)
        working-directory: mobile
        run: npx expo export --platform web --no-minify
        env:
          EXPO_PUBLIC_API_URL: https://fitzo.onrender.com/api
