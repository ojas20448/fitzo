node_modules/
.expo/
dist/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
/backend/.env
/mobile/.env*
/mobile/google-services-key.json
.DS_Store
coverage/
.vscode/
.idea/
.tdad/
*.bak
backend/scripts/exercisedb_full.json

# SDD scratch workspace (ledger, briefs, review packages)
.superpowers/

# Local media scratch folder (~834MB: source art, video projects, renders,
# and a vendored node_modules with a 194MB chrome-headless-shell binary).
# Git rejects files >100MB, and none of this is needed to build the app —
# shipped assets live in mobile/assets/. Keep this backed up outside git.
Media/

/RENDER_ENV_SETUP.md
